{
  "name": "@lifescale/shared",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"shared has no build step — bundled by consumers\""
  }
}
